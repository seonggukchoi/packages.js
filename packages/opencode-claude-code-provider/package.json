{
  "name": "@seonggukchoi/opencode-claude-code-provider",
  "version": "0.8.0",
  "private": true,
  "deprecated": "Deprecated and unmaintained. As of 2026-06-15 Anthropic bills `claude -p` / Claude Agent SDK usage — including third-party apps that authenticate with a Claude subscription — against a separate monthly Agent SDK credit at standard API rates instead of subscription usage limits, which removes this provider's reason to exist. See https://support.claude.com/en/articles/15036540-use-the-claude-agent-sdk-with-your-claude-plan",
  "description": "[DEPRECATED] OpenCode provider backed by Claude Code CLI — unmaintained; see deprecation notice",
  "author": "seonggukchoi",
  "license": "MIT",
  "type": "module",
  "engineStrict": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/seonggukchoi/packages.js",
    "directory": "packages/opencode-claude-code-provider"
  },
  "keywords": [
    "opencode",
    "claude",
    "provider",
    "ai-sdk"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rimraf dist .tsbuildinfo && tsc -p tsconfig.build.json",
    "lint": "eslint --fix .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:smoke": "node ./test/smoke.mjs",
    "test:typecheck": "tsc -p tsconfig.check.json --noEmit",
    "typecheck": "pnpm run test:typecheck"
  },
  "peerDependencies": {
    "@ai-sdk/provider": ">=3.0.0"
  },
  "dependencies": {
    "@ai-sdk/provider": "^3.0.8"
  },
  "devDependencies": {
    "@types/node": "^24.9.1"
  }
}
